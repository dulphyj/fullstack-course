import { UrlPaypalResponse } from './url-paypal-response';

describe('UrlPaypalResponse', () => {
  it('should create an instance', () => {
    expect(new UrlPaypalResponse()).toBeTruthy();
  });
});
